import Purple from '../images/background/purple.png';

export const painterFile = (name: string|null) => {
    switch (name) {
        case 'purple':
            return Purple;

        default:
            return Purple;
    }
};
